{
  "buildCommand": "echo \"GEMINI_API_KEY=$GEMINI_API_KEY\" > .env && git clone https://github.com/flutter/flutter.git -b stable --depth 1 && export PATH=\"$PATH:$(pwd)/flutter/bin\" && flutter pub get && flutter build web --release",
  "outputDirectory": "build/web"
}
